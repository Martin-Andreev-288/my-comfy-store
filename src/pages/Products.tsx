import { Filters, ProductsContainer, PaginationContainer } from "@/components";

const Products = () => {
  return (
    <>
      <Filters />
      <ProductsContainer />
      <PaginationContainer />
    </>
  );
};
export default Products;
